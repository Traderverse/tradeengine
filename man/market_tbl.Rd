% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/market_tbl.R
\name{market_tbl}
\alias{market_tbl}
\title{Create a Market Data Tibble}
\usage{
market_tbl(
  symbol,
  datetime,
  open,
  high,
  low,
  close,
  volume,
  adjusted = NULL,
  frequency = "daily",
  timezone = "UTC",
  asset_class = "equity"
)
}
\arguments{
\item{symbol}{Character vector of asset symbols/tickers}

\item{datetime}{POSIXct vector of timestamps (timezone-aware)}

\item{open}{Numeric vector of opening prices}

\item{high}{Numeric vector of high prices}

\item{low}{Numeric vector of low prices}

\item{close}{Numeric vector of closing prices}

\item{volume}{Numeric vector of trading volume}

\item{adjusted}{Numeric vector of adjusted closing prices (optional)}

\item{frequency}{Character string indicating data frequency (e.g., "1min", "1hour", "daily")}

\item{timezone}{Character string for timezone (default: "UTC")}

\item{asset_class}{Character string for asset class (e.g., "equity", "crypto", "futures", "fx")}
}
\value{
A \code{market_tbl} object (enhanced tibble) with standardized market data
}
\description{
Creates a standardized market data tibble (\code{market_tbl}) that serves as the
foundation for all tradeengine operations. This is the core data structure
used across the entire TradingVerse ecosystem.
}
\examples{
# Create daily stock data
prices <- market_tbl(
  symbol = rep("AAPL", 5),
  datetime = seq.POSIXt(as.POSIXct("2024-01-01"), by = "day", length.out = 5),
  open = c(150, 151, 152, 151, 153),
  high = c(152, 153, 154, 153, 155),
  low = c(149, 150, 151, 150, 152),
  close = c(151, 152, 153, 152, 154),
  volume = c(1e6, 1.1e6, 1.2e6, 1.15e6, 1.3e6)
)

# Create crypto data with 1-hour frequency
btc <- market_tbl(
  symbol = rep("BTC-USD", 24),
  datetime = seq.POSIXt(as.POSIXct("2024-01-01"), by = "hour", length.out = 24),
  open = rnorm(24, 50000, 500),
  high = rnorm(24, 50500, 500),
  low = rnorm(24, 49500, 500),
  close = rnorm(24, 50000, 500),
  volume = rnorm(24, 100, 10),
  frequency = "1hour",
  asset_class = "crypto"
)
}
