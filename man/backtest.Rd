% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{backtest}
\alias{backtest}
\title{Run Strategy Backtest}
\usage{
backtest(
  data,
  initial_capital = 10000,
  method = "vectorized",
  commission = NULL,
  slippage = NULL,
  position_size_method = "fixed",
  max_positions = 1,
  margin_requirement = 1,
  compound = TRUE
)
}
\arguments{
\item{data}{A \code{market_tbl} object with strategy signals (from \code{add_strategy()})}

\item{initial_capital}{Numeric value of starting capital}

\item{method}{Character: "vectorized" or "event_driven"}

\item{commission}{Numeric: commission rate (overrides strategy commission if provided)}

\item{slippage}{Numeric: slippage rate (overrides strategy slippage if provided)}

\item{position_size_method}{Character: position sizing method}

\item{max_positions}{Integer: maximum number of concurrent positions (default: 1)}

\item{margin_requirement}{Numeric: margin requirement as fraction (default: 1, no leverage)}

\item{compound}{Boolean: whether to compound returns (default: TRUE)}
}
\value{
A \code{backtest_results} object containing trades, equity curve, and statistics
}
\description{
Executes a complete backtest of a trading strategy on market data. Supports
both vectorized (fast, signal-based) and event-driven (realistic, order-based)
backtesting methods.
}
\examples{
\dontrun{
library(tradeengine)

# Run a simple backtest
results <- prices |>
  add_strategy(
    entry_rules = close > sma_20,
    exit_rules = close < sma_20
  ) |>
  backtest(initial_capital = 10000, method = "vectorized")

# View results
summary(results)
plot(results)
}
}
